spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/identity_db
    username: devuser
    password: devpass
    hikari:
      maximum-pool-size: 5
  jpa:
    hibernate:
      ddl-auto: none
  liquibase:
    enabled: true
